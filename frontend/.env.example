# Empty = same-origin requests, proxied to the API by Vite's dev server
# (see vite.config.ts). Works both at http://localhost:5173 and through a
# tunnel like ngrok, since the browser only ever talks to one origin.
# Set to an absolute URL (e.g. "http://localhost:4000") to bypass the proxy.
VITE_API_URL=""
