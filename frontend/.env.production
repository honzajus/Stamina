# Baked into any production build (`npm run build`), including the native
# iOS app bundle — Vercel's own deploy sets VITE_API_URL via its dashboard
# instead, so this file is what makes local/native release builds match it.
VITE_API_URL="https://api.5maidevs.lol"
